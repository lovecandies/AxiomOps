# ADR 003：为何不授予 Agent 恢复执行权限

## 背景

LLM 可能选择不安全动作或产生畸形参数；Prompt 指令本身不是授权边界。

## 决策

Agent 只诊断和发起恢复请求。后端审批通过后，由独立 Approver 和 Operator 在有限的 Sandbox 动作白名单内执行；恢复后再检查服务健康和订单链路。

## 影响

恢复速度低于 Agent 直接执行，但具备可强制的角色隔离、幂等、审计记录和回滚处理。
