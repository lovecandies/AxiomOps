# ADR 002：为何以当前 Evidence 作为 RCA 依据

## 背景

模型输出和历史相似案例都不能证明正在发生的 Incident。

## 决策

将每次类型化工具观测保存为带 SHA-256 摘要和元数据的不可变 JSON；要求 RCA 结论引用当前 Evidence ID，并由 Verifier 拒绝无证据支撑的结论。

## 影响

诊断变得可追溯、可复现。证据采集会增加存储及工具调用延迟，但能防止仅因回答听起来合理就被接受。
