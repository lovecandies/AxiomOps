# ADR 001：为何用 Agentic Workflow 做调查决策

## 背景

Incident 状态流转、持久化、审批与恢复属于确定性的后端工作。诊断不同：下一步需要哪类证据取决于当前故障症状及已采集证据。

## 决策

诊断使用 Agent Runtime，但以固定工作流、类型化工具契约、Evidence 持久化和独立验证器约束它。

## 影响

系统可调整调查顺序，却不允许任意访问基础设施。相比固定工作流，这会增加延迟和模型调用，因此只用于需要可审计诊断的 Incident。
