# Phase 8 Frontend Boundary

- This directory is a Vite React TypeScript control console only.
- Call the FastAPI control plane at `VITE_API_BASE_URL` (default `http://127.0.0.1:18000`).
- Never implement recovery policy, role checks, or rollback behavior in the browser.
- Keep the demo focused on one inventory-fault incident and three explicit human roles.
